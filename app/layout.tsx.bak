import "./globals.css";
import type { ReactNode } from "react";
import type { Metadata } from "next";
import Link from "next/link";

export const meta Metadata = {
  title: {
    default: "Paper H2H Lab",
    template: "%s | Paper H2H Lab"
  },
  description:
    "Paper H2H Lab – a paper-style planning tool for FPL head-to-head leagues, focusing on doubles, blanks, and rival risk."
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="en">
      <body>
        <div className="min-h-screen flex flex-col">
          <header className="border-b border-slate-800 bg-slate-900/70 backdrop-blur">
            <div className="mx-auto max-w-7xl px-4 py-3 flex items-center justify-between">
              <Link
                href="/"
                className="text-lg font-semibold text-primary"
              >
                Paper H2H Lab
              </Link>
              <nav className="flex gap-4 text-sm text-slate-300">
                <Link href="/planner">Planner</Link>
                <a
                  href="https://fantasy.premierleague.com/"
                  target="_blank"
                  rel="noreferrer"
                  className="text-slate-400 hover:text-slate-200"
                >
                  Official FPL
                </a>
              </nav>
            </div>
          </header>
          <main className="flex-1">{children}</main>
          <footer className="border-t border-slate-800 text-xs text-slate-500 px-4 py-3">
            <div className="mx-auto max-w-7xl flex justify-between">
              <span>Paper H2H Lab MVP</span>
              <span>All data is mock only.</span>
            </div>
          </footer>
        </div>
      </body>
    </html>
  );
}
